# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Documentation toolchain (used by the gh-pages workflow to build guides/ into
# docs/ via `bake utopia:project:static`).
group :maintenance, optional: true do
  gem "utopia-project"
end
